.coach_subheader
  .w-container
    .section.header
      .w-row
        .w-col.w-col-8.nav-bar
          a.nav-link href="/" = t("coach.subheader.dashboard")
          a.nav-link href="/manage_teams" = t("coach.subheader.manage_teams")
          a.nav-link href="/coaches/#{current_user.id}" = t("coach.subheader.profile")